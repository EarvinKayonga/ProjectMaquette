# ProjectMaquette

### Context

School project


### Setup

```
apt-get install build-essential nodejs-legacy git
git clone [this repo]
cd ProjectMaquette
npm install; npm start;

go to localhost:10002

```